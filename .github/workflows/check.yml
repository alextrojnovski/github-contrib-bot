name: Daily GitHub Contribution Check

on:
  schedule:
    - cron: '0 17 * * *'
  workflow_dispatch:

jobs:
  check-contributions:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}  # Встроенный токен!
    
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'
        cache: true
    
    - name: Install dependencies
      run: go mod download
    
    - name: Install SQLite
      run: |
        sudo apt-get update
        sudo apt-get install -y sqlite3 libsqlite3-dev gcc
    
    - name: Run contribution checker
      run: go run .
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GH_USERNAME: ${{ secrets.GH_USERNAME }}  # Это твой логин!
    
    - name: Commit and push database
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Встроенный токен!
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add commits.db
        git diff --quiet && git diff --staged --quiet || git commit -m "Update contribution stats [skip ci]"
        git push